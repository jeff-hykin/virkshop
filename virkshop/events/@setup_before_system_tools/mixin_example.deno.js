export const deadlines = {
    async beforeSetup(virkshop) {
    },
    async beforeReadingSystemTools(virkshop) {
    },
    async beforeShellScripts(virkshop) {
    },
    async beforeEnteringVirkshop(virkshop) {
        // if you CAN put code here (instead of the other areas) please do
        // its the most efficient way, the other methods are just if you NEED
        // to create something before something else happens
    },
}
